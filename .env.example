# =============================================================================
# CONVEX DEPLOYMENT
# =============================================================================
# Keep the deployment key secret; store only in .env.local
CONVEX_DEPLOYMENT_KEY=
CONVEX_DEPLOYMENT_URL=https://marvelous-bat-438.convex.cloud
CONVEX_SITE_URL=https://marvelous-bat-438.convex.site

# Next.js client-side Convex endpoints
NEXT_PUBLIC_CONVEX_URL=https://marvelous-bat-438.convex.cloud
NEXT_PUBLIC_CONVEX_SITE_URL=https://marvelous-bat-438.convex.site

# =============================================================================
# NEXT.JS ENVIRONMENT VARIABLES (set in .env.local)
# =============================================================================
# App URL used for Better Auth callbacks on the client side
# For production: https://film.flmlnk.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# CONVEX ENVIRONMENT VARIABLES (REQUIRED - set with: npx convex env set)
# =============================================================================
# These MUST be set in Convex, not in .env.local!
# The 403 auth errors occur when these are missing.
#
# Run these commands to set up authentication:
#   npx convex env set BETTER_AUTH_SECRET "$(openssl rand -base64 32)"
#   npx convex env set SITE_URL "http://localhost:3000"  # or your production URL
#   npx convex env set GOOGLE_CLIENT_ID "your-google-client-id"
#   npx convex env set GOOGLE_CLIENT_SECRET "your-google-client-secret"
#
# BETTER_AUTH_SECRET - Required for session encryption (generate with: openssl rand -base64 32)
# SITE_URL - Your app URL for auth callbacks (http://localhost:3000 for dev)
# GOOGLE_CLIENT_ID - From Google Cloud Console (for Google OAuth)
# GOOGLE_CLIENT_SECRET - From Google Cloud Console (for Google OAuth)

# Resend API key for email sending
# Get your API key from https://resend.com/api-keys
# NOTE: This must be set as a Convex environment variable (not Next.js)
# Run: npx convex env set RESEND_API_KEY re_xxxxx
RESEND_API_KEY=

# Resend Webhook Secret for email event tracking (opens, clicks, bounces)
# Setup:
# 1. Go to Resend Dashboard > Webhooks
# 2. Add endpoint: https://your-convex-deployment.convex.site/resend/webhook
# 3. Select events: email.sent, email.delivered, email.opened, email.clicked, email.bounced, email.complained
# 4. Copy the signing secret (starts with whsec_)
# Run: npx convex env set RESEND_WEBHOOK_SECRET whsec_xxxxx
RESEND_WEBHOOK_SECRET=

# OpenAI API key for AI-powered email generation
# Get your API key from https://platform.openai.com/api-keys
# Run: npx convex env set OPENAI_API_KEY sk-xxxxx
OPENAI_API_KEY=

# =============================================================================
# ELEVENLABS CONFIGURATION (Trailer Audio Generation)
# =============================================================================
# ElevenLabs API key for AI-generated trailer music and sound effects
# Get your API key from https://elevenlabs.io/settings/api
# This is used by the Modal trailer processing pipeline for:
#   - Generating cinematic trailer music
#   - Creating sound effects (impacts, risers, whooshes)
#   - Audio mixing and mastering
#
# Set as Convex environment variable (same as OPENAI_API_KEY):
# Run: npx convex env set ELEVENLABS_API_KEY xi_xxxxx
#
# Optional: Audio generation will be skipped if not set
ELEVENLABS_API_KEY=

# =============================================================================
# ANALYTICS CONFIGURATION
# =============================================================================

# Google Tag Manager Container ID (client-side tracking)
# Get from https://tagmanager.google.com/
# Format: GTM-XXXXXXX
NEXT_PUBLIC_GTM_ID=

# GA4 Data API Configuration (server-side - set in Convex)
# These are for pulling analytics data back into the platform
#
# Get Property ID from: GA4 Admin → Property Settings (numeric ID)
# Run: npx convex env set GA4_PROPERTY_ID 123456789
#
# Create service account at: https://console.cloud.google.com/
# Enable "Google Analytics Data API", create service account with Viewer role
# Download JSON key and set the entire contents:
# Run: npx convex env set GA4_SERVICE_ACCOUNT_KEY '{"type":"service_account",...}'

